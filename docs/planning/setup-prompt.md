# Historical bootstrap prompt (relocated)

The Day-0 bootstrap prompt contained operator-specific context (identity,
ventures, geography). It now lives only in the private companion repo:

`ai-memory-infra-private/docs/planning/setup-prompt.md`

For current design and next actions, read `docs/architecture.md`, `AGENTS.md`, and
`docs/planning/STATUS.md`.
