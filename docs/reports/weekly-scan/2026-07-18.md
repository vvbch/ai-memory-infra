# Repo scan & architecture benchmark — 2026-07-18

> Weekly maintenance agent (CI, headless, this repo only). Diffs against
> `2026-06-10-baseline.md`. All changes in this PR are two-way doors.

## Summary

- Gates green after fixes: ruff (was 17 errors), mypy (3 errors), pytest 220 / 88.6% cov, memory-contract + STATUS + eval gate OK.
- Doc-side tenet-10 cluster fixed: CD/CORS/Phase-6 wording still claimed “live” in architecture/interfaces/setup/market-fit; aligned with honest `AGENTS.md` practices tags.
- ADR 009 now carries an **implementation status** table (PII/CORS/rate-limit = target); ADR 032 notes in-memory LifeGraph POC landed.
- Benchmark: same strengths as baseline (DR, COEs, docs-as-code, cost); eval CI gate and remote MCP closed big gaps; supply-chain pinning + CD + SLOs still below bar.
- No one-way-door changes applied (no droplet, Terraform, retention, spend, or live data).

## Test / gate results

| Check | Result |
|---|---|
| `ruff check .` | **fixed** (import order, E402/E501/SIM115/F401, mypy-related cleanup) → clean |
| `mypy src` | **fixed** (`health/checker.py` None status; `life_graph/ingest.py` meta narrowing) → clean |
| `pytest` | 220 passed, coverage 88.61% (gate 80%) |
| `check_memory_contract.py` | OK (extension skipped — not checked out) |
| `check_status_snapshot.py` | OK (~70 lines) |
| `run_eval_gate.py` | PASSED (synthetic gold) |

## Findings fixed in this PR (two-way)

1. **Lint/type debt** in scripts + `src/health`, `src/life_graph`, `src/migration/categorizer`.
2. **`docs/architecture.md`** — CD claim → CI + manual deploy; CORS → target; Neo4j/LifeGraph POC wording; `mcp.` subdomain + mermaid edge; DNS list includes `mcp.`.
3. **`docs/interfaces.md` §5/§7** — CORS marked target; `mcp.` in route contract.
4. **`docs/setup.md`** — removed “merge → CD deploys.”
5. **`docs/market-fit-validation.md`** — remote MCP live; guardrails/CD honesty.
6. **ADR 009** — partial-implementation status table; **ADR 032** — POC update note.
7. **`BACKLOG.md` P1 docs-drift** — trimmed stale “Actions (1)(2)(3)” already done.
8. **`STATUS.md`** — Phase 9 “largely done” → “ongoing” (matches AGENTS/README).

## Findings needing an operator decision (NOT applied)

| Finding | Recommendation |
|---|---|
| No lockfile; `pyproject.toml` uses `>=` ranges | Add `uv lock` (or pip-tools) + Dependabot. Two-way once chosen; pick tool deliberately (tenet 12). |
| Actions pinned by tag (`@v4`/`@v5`), not SHA | Pin to commit SHAs (OpenSSF). Cheap two-way; do with lockfile pass. |
| PII filter only in `src/eval/guardrails.py` | Wire into write path **or** keep eval-only. Wiring is product decision; stay target until approved. |
| Caddy CORS commented; rate limit needs xcaddy | Confirm Mem0 `DASHBOARD_URL` enough for CORS; rate limit stays BACKLOG unless custom build spend approved (one-way-ish spend). |
| No `cd.yml` | Keep manual `make deploy` until health-check + rollback designed; don’t imply CD. |
| LifeGraph redesign / Neo4j seed | Freeze POC per STATUS; don’t seed Neo4j until HLD approved (one-way data plane). |
| No SLOs / error budgets | Define 1–2 SLOs (API availability, backup freshness) after ~1 week steady usage — track in BACKLOG, don’t invent numbers now. |
| RTO never timed end-to-end | Operator: time one full restore on a throwaway path; record minutes in runbook. Droplet-side — out of this agent’s scope. |

## Benchmark scorecard

| Area | Status | Gap / next step |
|---|---|---|
| Twelve-Factor | **Meets** | env config, stateless sessions (tenet 16); keep |
| SRE / COEs | **Exceeds for size** | 18 COEs; still no SLOs/error budgets → define when usage real (**two-way** doc) |
| Backup / DR | **Meets** | RPO 24h + monthly restore drill; **gap:** publish measured RTO (**operator**, droplet) |
| CI / DORA | **Baseline+** | Real `ci.yml` + eval gate + weekly-scan + ci-autofix; **no CD** → `cd.yml` when ready (**target**) |
| Supply chain | **Below bar** | gitleaks strong; missing lockfile, Dependabot, action SHA-pin (**two-way**, recommend next) |
| OWASP ASVS L1 | **Partial** | HTTPS + JWT/API-key + basic-auth UIs; PII/CORS/rate-limit target (ADR 009 now honest) |
| Docs-as-code | **Meets** | STATUS/contract/pointer/eval gates enforced in CI |
| Eval / model quality | **Partial → better** | Synthetic gold **blocking in CI** (was stubs at baseline); live-stack eval still **[target]** |
| Cost / FinOps | **Meets** | landed-cost ×1.3, flat-rate bias, caps |
| Agent autonomy safety | **Strong** | PR-only weekly agent, reversibility rules, zero live credentials |

## Redundancy / consistency notes

- Fixed: architecture/interfaces/setup/market-fit present-tense CD/CORS/Phase-6 vs AGENTS.
- Intentional: STATUS vs BUILD-JOURNEY (snapshot vs curated public log); ADR history + correction banners.
- Ambiguous left alone: Phase 6 “✅ POC” in AGENTS vs STATUS “redesign from scratch” — both true if read as code-done / product-pivot; no further edit without operator.

## Deltas since 2026-06-10 baseline

| Topic | Baseline | Now |
|---|---|---|
| pytest | 107 / ~94% | 220 / 88.6% (real modules, not empty stubs) |
| CI | newly real | still real + eval gate + contract render + pointer purity |
| Eval | stubs / below bar | synthetic gold **blocking**; live-stack still target |
| Security docs honesty | AGENTS reword recommended | AGENTS already honest; arch/interfaces/market-fit **fixed this week**; ADR 009 status table added |
| Workflow stubs (`cd.yml` etc.) | delete or implement | stubs **gone**; only real workflows remain |
| Remote MCP | planned | **live** (ADR 034/035; architecture coverage matrix) |
| LifeGraph | “Phase 6 not built” | in-memory POC ✅; Neo4j empty; redesign pending |
| Supply-chain / CD / SLOs | below bar | **unchanged gaps** — still top operator recommendations |
