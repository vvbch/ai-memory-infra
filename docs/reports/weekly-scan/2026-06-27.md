# Weekly repo scan — 2026-06-27

> Headless CI run (fresh checkout, no droplet/API credentials). Changes land via
> this workflow's PR — not pushed to main directly.

## Summary

- **Gates were red on main:** 17 ruff + 3 mypy failures accumulated since the
  2026-06-10 baseline; all fixed in this PR (two-way doors).
- **Doc drift corrected:** CORS allowlist and CI/CD claims in `architecture.md`,
  `interfaces.md`, and `market-fit-validation.md` now match `AGENTS.md` honest
  `[in place]` / `[target]` tags.
- **Tests green:** 220 passed, 89% `src/` coverage; memory-contract + STATUS
  snapshot gates OK.
- **Maturity up since baseline:** `eval-suite.yml` is live; stub workflows
  removed; Phases 5–8 code is real (not empty stubs).
- **Still below bar:** supply-chain pinning, push CD, SLOs, live-stack eval,
  PII filter / CORS / rate limiting — operator decisions below.

## Test / gate results

| Check | Result |
|---|---|
| `ruff check .` | **FAIL → PASS** (17 issues fixed) |
| `mypy src` | **FAIL → PASS** (3 type errors fixed) |
| `pytest` | 220 passed, 88.6% coverage (gate 80%) |
| `check_memory_contract.py` | OK (extension repo skipped — not checked out) |
| `check_status_snapshot.py` | OK (69 lines) |
| `render_contract.py --check` | OK |

Root cause of lint regression: new/changed scripts (`memory.py`, compaction,
bulk importer, MCP proxy) merged without a local `ruff check .` pass; CI should
have blocked — verify the failing commit landed before CI covered those paths.

## Findings fixed in this PR

1. **Ruff (17):** import sorting (`client.py`, `server.py`, tests), unused
   imports, E501 line wraps, SIM115 file context managers, E402 noqa on path-
   bootstrap scripts.
2. **Mypy (3):** `check_http_status` guards `None` status; `ingest_memory_record`
   uses a typed `meta` dict.
3. **Tenet-10 doc drift:** `architecture.md` subdomain table + cost row — CORS
   marked **[target]**; GitHub row no longer claims CD. `interfaces.md` §5/§7 —
   CORS aspirational language clarified. `market-fit-validation.md` — guardrails
   and CI/CD rows honest (⚠️ / split CI vs CD).

## Findings needing an operator decision

| Finding | Recommendation |
|---|---|
| No lockfile; `pyproject.toml` uses `>=` ranges | Add `uv.lock` or `pip-tools` lock + Renovate/Dependabot — **two-way door**, do when ready |
| GitHub Actions pinned by tag (`@v4`), not SHA | Pin to commit SHAs once workflow set stabilizes — **two-way door** |
| PII filter, CORS allowlist, Caddy rate limiting not deployed | Implement per ADR 009 Phase 9 plan, or leave as **[target]** everywhere (docs now honest) — **one-way door** when live |
| `MEM0_DEFAULT_LLM_MODEL` silent fallback to `gpt-4.1-nano` | Boot-time assert or alert on droplet — **one-way door** (live config) |
| RTO never measured | Time a full restore drill once; record in runbook — **one-way door** (runs against live backup) |
| LifeGraph redesign blocked on operator workshop | Per STATUS — not a scan fix; schedule HLD session |

**Ambiguous (not changed):** `docs/design/lifegraph.md` §1 still calls legacy POC
pieces "stubs" — accurate for the frozen redesign context, but differs from
`AGENTS.md` "Phase 6 ✅ in-memory POC." Keep both; add a cross-link if confusing.
Historical COEs referencing empty `src/` stubs were left intact (audit trail).

## Benchmark scorecard

| Area | Status | Gap | Next step |
|---|---|---|---|
| Twelve-Factor | **Meets** | — | Keep env-based config |
| Google SRE (SLOs/EB) | **Gap** | No SLOs or error budgets | Define API availability + backup freshness SLOs — **recommend** |
| Google SRE (postmortems) | **Exceeds for size** | — | 9+ COEs with 5-whys |
| Backup / DR | **Meets design** | RTO unmeasured | Time one restore drill — **one-way door** |
| DORA / CI | **Baseline CI** | No CD; manual SSH deploy | `cd.yml` with health check + rollback — **recommend** |
| Supply chain | **Below bar** | No lockfile, unpinned actions/deps | Lockfile + SHA-pin + Dependabot — **two-way when prioritized** |
| OWASP ASVS L1 | **Partial** | No rate limit / CORS / PII filter live | Phase 9 security backlog |
| Docs-as-code | **Meets** | — | STATUS + contract gates enforced in CI |
| LLM eval | **Partly meets** | Synthetic gold only; no live-stack eval | Expand gold + live eval when stack stable — **recommend** |
| Secret scanning | **Meets** | gitleaks in hook installer docs | — |
| Agent safety (this workflow) | **Meets** | — | PR-only, reversibility classification |

## Deltas since 2026-06-10 baseline

| Metric | Baseline | Today |
|---|---|---|
| pytest count | 107 | 220 |
| Coverage | 93.7% | 88.6% (more code; still above gate) |
| ruff on main | 1 failure (fixed in session) | 17 failures (fixed this PR) |
| mypy | clean | 3 errors (fixed this PR) |
| `eval-suite.yml` | comment stub | **live** weekly workflow |
| Stub workflows (`cd`, `backup-verify`, etc.) | present | **deleted** |
| `src/eval/` | described as stubs | implemented + CI gate |
| Security claims in `AGENTS.md` | overstated | already corrected to partly [in place] |
| Doc CORS/CD claims | overstated in architecture | **corrected this PR** |
