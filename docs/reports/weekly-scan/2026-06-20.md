# Weekly scan — 2026-06-20

> Headless CI run (fresh checkout, no droplet/credentials/sibling repos).

## Summary

- **CI was red:** 17 ruff + 3 mypy failures on main — all fixed in this PR (two-way doors).
- **Tests green:** 220 passed, 89% `src/` coverage; all contract/render/pointer gates OK.
- **Doc drift fixed:** `market-fit-validation.md` overstated PII/guardrails; `architecture.md` CORS note clarified.
- **Baseline gaps closed:** `eval-suite.yml` is live (was a stub); comment-only `cd.yml`/`backup-verify.yml`/`docker-build.yml` stubs are gone.
- **Still below bar:** supply-chain pinning, CD automation, SLOs, and ADR 009 aspirational security items.

## Test / gate results

| Check | Result |
|---|---|
| `ruff check .` | **FAIL → fixed** (17 issues: import order, E501, SIM115, unused imports) |
| `mypy src` | **FAIL → fixed** (3: `checker.py` None guard, `ingest.py` meta typing) |
| `pytest` | 220 passed, 89% coverage (gate 80%) |
| `check_memory_contract.py` | OK (extension repo skipped — not checked out) |
| `check_status_snapshot.py` | OK (69 lines) |
| `render_contract.py --check` | OK |
| `check_pointer_purity.py` | OK |

## Findings fixed in this PR

1. **Lint/type regressions** — ruff clean + mypy strict across `src/`; script import blocks (`E402`/`I001`) and line-length/SIM115 fixes in `scripts/` and `src/migration/categorizer.py`.
2. **`market-fit-validation.md`** — Safety/guardrails row changed from ✅ "PII filter verified" to ⚠️ honest target-state (matches `AGENTS.md` + `interfaces.md` §5).
3. **`architecture.md`** — Subdomain table: `memory.` row no longer implies a full CORS allowlist is shipped; notes `DASHBOARD_URL` env + BACKLOG hardening.

## Findings needing an operator decision

| Finding | Recommendation |
|---|---|
| **Supply chain** — no lockfile, `>=` ranges in `pyproject.toml`, Actions pinned by tag (`@v4`/`@v5`) | Add `uv.lock` or `requirements.lock` + Dependabot/Renovate; pin action SHAs once workflows stabilize. Two-way in repo; operator picks tooling. |
| **ADR 009 aspirational items** — PII filter, Caddy rate limiting, full CORS hardening described as architecture but not built | Keep `AGENTS.md` honest tags; optionally add ADR 009 status banner ("target — Phase 9") so portfolio readers don't over-read. Implement per phase plan. |
| **CD** — deploys remain manual SSH (`make deploy`); no `cd.yml` | Build push-to-main CD with health check + rollback when LifeGraph thread allows ops focus. One-way until tested on droplet. |
| **SLOs / error budgets** — no defined availability or backup-freshness SLOs | Define 1–2 SLOs (API uptime, backup age) once LifeGraph redesign stabilizes usage. Doc-only first. |
| **RTO** — restore drills run monthly; full RTO never timed | Time one end-to-end restore on droplet; record in runbook. Requires live env (out of scan scope). |
| **`gpt-4.1-nano` silent fallback** if `MEM0_DEFAULT_LLM_MODEL` unset | Boot-time assert or alert on live config. Droplet-only. |
| **LifeGraph redesign** — in-memory POC frozen; Neo4j empty | Continue `docs/design/lifegraph.md` workshop per STATUS next action; ADR if model changes. |

## Benchmark scorecard

| Area | Status | Gap | Next step |
|---|---|---|---|
| Twelve-Factor (config, stateless) | **Meets** | — | Keep env-based config; no change |
| SRE (COE/postmortems) | **Exceeds for size** | No SLOs/error budgets | Draft 1–2 SLOs in `docs/runbook.md` (two-way) |
| Backup / DR | **Meets** | RTO unmeasured | Time restore on droplet (one-way/live) |
| CI (DORA) | **Meets** | No ephemeral Docker stack in CI | Track as `[target]` in practices.yaml |
| CD (DORA) | **Below bar** | Manual SSH deploy | Implement `cd.yml` when ready (one-way) |
| Supply chain | **Below bar** | No lockfile, dep pinning, action SHAs, Dependabot | Lockfile + Renovate PR (two-way) |
| API security (OWASP ASVS L1) | **Partial** | PII filter, rate limit, CORS hardening | Phase 9 security backlog |
| Docs-as-code | **Meets** | — | Contract gates + weekly scan sustain |
| Eval / model quality | **Partly meets** | Synthetic gold only; no live-stack eval | Expand gold + live eval per ADR 007 (tracked) |
| Observability | **Meets design** | `/metrics` not on live Mem0 server | VPS `make deploy-obs` (operator, parked) |

## Redundancy / consistency notes

- **STATUS vs BUILD-LOG/BUILD-JOURNEY:** intentional split; STATUS is snapshot-shaped (gate OK). No trim needed.
- **ADR 009 vs AGENTS.md vs interfaces.md:** AGENTS/interfaces agree (partly in place); ADR 009 prose still reads as shipped guardrails — flag for optional ADR status banner (ambiguous; operator call above).
- **`import_gdrive.py`:** one-line stub — accurate per `docs/design/migration-pipeline.md`; not dead file drift.
- **Neo4j role:** `architecture.md` ADR 032 correction holds; matches `infra/` (Neo4j in compose, Mem0 doesn't write graph).

## Deltas since 2026-06-10 baseline

| Baseline (2026-06-10) | Now (2026-06-20) |
|---|---|
| 107 tests, 94% coverage | **220 tests**, 89% coverage (more `src/` surface) |
| ruff/mypy clean at scan | **Regressed** — 20 lint/type issues accumulated; fixed here |
| `eval-suite.yml` stub | **Live** — Sunday 03:30 UTC cron + artifact upload |
| `cd.yml` etc. comment stubs | **Removed** from repo |
| `src/eval/` "stubs" | **Implemented** — metrics, gold data, blocking CI gate |
| Neo4j docs overstated | **Mostly corrected** (ADR 032); LifeGraph redesign thread open |
| First weekly-scan report | This is scan #2 |
