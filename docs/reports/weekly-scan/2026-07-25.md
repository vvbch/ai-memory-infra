# Weekly scan — 2026-07-25

> Headless CI maintenance pass. Fresh checkout only — no droplet, no live API,
> no sibling repos. Two-way doors only.

## Summary

- Gates green after fixes: ruff, mypy (strict), pytest 220/220 (88.7% cov),
  memory-contract, STATUS snapshot.
- Doc drift closed: CORS/CD overclaims, LifeGraph “not yet built” vs POC,
  market-fit honesty, BACKLOG Phase-2 restore-drill stale “open” item.
- Benchmark unchanged at the top: strong DR/COE/docs-as-code; still below bar
  on supply-chain pinning, SLOs, and CD.
- Eval: synthetic gold gate is real (CI + `eval-suite.yml`); live-stack eval
  still target.
- No one-way-door changes applied; operator decisions listed below.

## Test / gate results

| Check | Result |
|---|---|
| `ruff check .` | fail → **fixed** → pass |
| `mypy src` | fail (3) → **fixed** → pass |
| `pytest` | **220 passed**, cov 88.7% (gate 80%) |
| `check_memory_contract.py` | OK (extension repo skipped — not checked out) |
| `check_status_snapshot.py` | OK (STATUS 69 lines) |

## Findings fixed in this PR

1. **Ruff** — import order / E402 / E501 / SIM115 / unused import across
   `scripts/{acceptance_probe,bulk_seed_importer,memory,memory_compaction}.py`,
   `src/migration/categorizer.py`, and related auto-fixes.
2. **Mypy** — `health/checker.py` None-safe `check_http_status`;
   `life_graph/ingest.py` narrow metadata dict; test assert for empty response.
3. **`docs/architecture.md`** — remote MCP edge now live (not “future”);
   LifeGraph POC + redesign note; GitHub row no longer claims CD; subdomain
   CORS marked **[target]**.
4. **`docs/interfaces.md` §5/§7** — CORS allowlist honesty aligned with
   `AGENTS.md` security practice tags.
5. **`docs/market-fit-validation.md`** — MCP live; PII/rate/CORS and CD
   demoted from false greens; dropped stale Bangalore job count.
6. **`docs/setup.md`** — Dockerfile graph extras called inert (ADR 032).
7. **ADR 005 / 032** — status lines updated: in-memory POC exists; live Neo4j
   seed still outstanding.
8. **`BACKLOG.md`** — Phase 2 backup/restore marked DONE (was still “§4
   remains” despite AGENTS/BUILD-JOURNEY close-out).

## Findings needing an operator decision

| Finding | Recommendation |
|---|---|
| No lockfile; deps use `>=` ranges; Actions pinned by tag (`@v4`/`@v5`/`@v7`), not SHA; no Dependabot | **Two-way:** add `uv lock` (or pip-tools) + Dependabot + SHA-pin Actions when workflows stabilize. Do in a dedicated supply-chain PR. |
| No push-to-main CD (`cd.yml`); deploys remain manual SSH | Keep as **[target]** until health-check + rollback design is ready; do not pretend CD exists. |
| CORS allowlist / rate limit / PII filter still aspirational (ADR 009) | Implement in Phase 9/security order, or leave tagged **[target]** (docs now honest). Rate limit needs custom Caddy build — deliberate spend/complexity call (tenet 12). |
| `MEM0_DEFAULT_LLM_MODEL` boot-assert still open (COE 2026-06-10) | Droplet/config change — **out of this runner’s scope**. Add assert on next ops session. |
| Graph-source one-way door (LifeGraph-only vs graph-capable Mem0) before live Neo4j seed (ADR 032 §4) | Decide in LifeGraph redesign workshop (`docs/design/lifegraph.md`); do not seed Neo4j until decided. |
| `STATUS.md` still carries live hostnames (`memory.chandrav.dev`) in a public repo | Ambiguous: useful for resume vs public `example.com` convention. Recommend sanitize to env-override language if portfolio visitors matter; else leave. |
| Empty stub `src/migration/import_gdrive.py` (1-line placeholder) | Keep until GDrive import is scoped, or delete and note in BACKLOG — low priority. |
| Glob-scoped Cursor helpers (`10-python-tdd.mdc` / `20-docs-dod.mdc`) partially duplicate AGENTS | Already BACKLOG’d under ADR 033 — decide carve-out vs pure pointers. |

## Benchmark scorecard

| Area | Status | Gap / next step |
|---|---|---|
| Twelve-Factor | **Meets** | env config, stateless sessions (tenet 16), disposable VPS |
| SRE / postmortems | **Exceeds for size** | 20 COEs; **gap:** no SLOs/error budgets — define API availability + backup freshness once usage is steady (**two-way** doc when ready) |
| Backup / DR | **Meets** | RPO 24h, dead-man’s-switch, monthly restore drill; **gap:** measure RTO once (**ops**, droplet) |
| CI / DORA | **Baseline** | Real CI + weekly eval + weekly-scan; **gap:** no CD → manual SSH (**target**, don’t fake) |
| Supply chain | **Below bar** | gitleaks yes; no lockfile / SHA pins / Dependabot (**two-way**, apply when cheap) |
| OWASP ASVS L1 (API) | **Partial** | HTTPS + JWT/API key + basic-auth UIs; missing rate limit, full CORS allowlist, PII filter |
| Docs-as-code | **Meets** | STATUS + memory-contract + contract-render + pointer-purity gates |
| Eval / model quality | **Partial** | Synthetic gold blocking; live-stack + expanded gold **[target]** |
| Cost governance | **Meets** | landed-cost ×1.3, flat-rate bias, caps |
| Agent autonomy safety | **Strong** | zero-cred runners, PR-only, reversibility classification |

## Deltas since 2026-06-10 baseline

- Test count **107 → 220**; coverage still well above 80%.
- Baseline’s stub workflows: **gone**; `eval-suite.yml` and `weekly-scan.yml` are real.
- `AGENTS.md` practice tags now honest (`[in place]` / `[target]`) — baseline’s biggest credibility gap largely closed.
- Neo4j dual-namespace drift corrected earlier (ADR 032); this scan closed residual “Phase 6 not built” / CORS / CD wording.
- New open product thread: **LifeGraph redesign** (STATUS 2026-06-16) — Neo4j still 0 nodes.
- Supply-chain / SLO / CD gaps: **unchanged** since baseline.
- COE count: baseline cited 9; repo now has **20** under `docs/coe/`.
