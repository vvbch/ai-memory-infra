# Repo scan & architecture benchmark — 2026-07-11

> Weekly maintenance agent (CI, headless, this repo only). Two-way doors only.

## Summary

- Gates green after lint/type fixes: ruff clean, mypy clean, **220** pytest (88.6% cov), memory-contract OK, STATUS snapshot OK.
- Cleared tenet-10 doc drift: architecture/interfaces/setup/market-fit overstated CD, CORS, and LifeGraph/Neo4j status; `user_id` typo `"the operatorv"` → `primary-user`.
- Baseline “workflow stubs” finding is **resolved** — `eval-suite.yml` / `weekly-scan.yml` / `ci-autofix.yml` are real; still no `cd.yml`.
- Supply-chain and ASVS gaps unchanged (no lockfile, tag-pinned Actions, PII/CORS/rate-limit still target).
- STATUS left as LifeGraph redesign handoff (2026-06-16); not overwritten by this scan.

## Test / gate results

| Check | Result |
|---|---|
| `ruff check .` | fail → **fixed** (import order, E402/E501/SIM115, unused imports) |
| `mypy src` | fail → **fixed** (`health/checker.py` None status; `life_graph/ingest.py` meta typing) |
| `pytest` | **220 passed**, coverage 88.61% (gate 80%) |
| `check_memory_contract.py` | OK (extension repo skipped — not checked out) |
| `check_status_snapshot.py` | OK (69 lines) |

## Findings fixed in this PR

1. **Lint/type debt** in `scripts/{acceptance_probe,bulk_seed_importer,memory,memory_compaction}.py`, `src/mcp_proxy/{client,server}.py`, `src/migration/categorizer.py`, `src/health/checker.py`, `src/life_graph/ingest.py`, two tests.
2. **`docs/architecture.md`** — remote MCP live (not “future”); `mcp-proxy` + profile-gated dash/obs; CD/CORS honesty; LifeGraph = in-memory POC frozen, Neo4j still unused.
3. **`docs/interfaces.md`** — `user_id` → `primary-user`; CORS marked **[target]**; `mcp.` in route contract.
4. **`docs/setup.md`** — “merge → CD deploys” → manual `make deploy`.
5. **`docs/market-fit-validation.md`** — remote MCP live; PII/CD honesty tags.
6. **`AGENTS.md` / `README.md` / ADR 032** — as-of 2026-07-11; LifeGraph POC frozen pending redesign.
7. **`docs/planning/BACKLOG.md`** — Phase 2 §4 restore drill marked done (matches ADR 023).

## Findings needing an operator decision

| Finding | Recommendation |
|---|---|
| **ADR 009** still lists write-path PII filter, CORS allowlist, Caddy rate limiting as Decision; AGENTS correctly tags them **[target]**; guardrails live only in `src/eval/` | Amend ADR 009 with “Accepted / partially implemented” + target checklist (cheap, honest) **or** schedule Phase-9 implementation. Prefer amend-now. |
| **Mem0 `/metrics`** scraped by Prometheus but absent on server (STATUS blocker) | Keep “expected DOWN” until Mem0 image patch, or drop scrape job. Droplet work — out of scan scope. |
| **No lockfile / Dependabot; Actions `@v4`/`@v5` not SHA-pinned** | Add `uv lock` (or pip-tools) + Dependabot; pin Actions to SHAs once workflows stabilize. Two-way door but touches supply-chain policy — operator call. |
| **CD still absent** | Keep manual SSH until a watched `cd.yml` (health check + rollback) is worth the ops load. Do not stub. |
| **LifeGraph phase taxonomy** | STATUS = redesign workshop; AGENTS Phase 6 ✅ frozen. Confirm whether redesign reopens Phase 6 or is a new track before rewriting the phase map further. |
| **`gpt-4.1-nano` silent fallback** if live `MEM0_DEFAULT_LLM_MODEL` unset | Boot-time assert on droplet (out of scan scope). |

## Benchmark scorecard

| Area | Status | Gap / next step |
|---|---|---|
| Twelve-Factor | **Meets** | env config, stateless sessions (tenet 16), disposable infra |
| Google SRE / COEs | **Exceeds for size** | strong blameless COEs; still no SLOs/error budgets — define 1–2 once usage is steady (**one-way-ish product call**) |
| Backup / DR | **Meets** | RPO 24h + dead-man's-switch + monthly drill (ADR 023); gap: measure RTO once (**two-way**, ops) |
| CI/CD (DORA) | **CI meets / CD below** | CI on every push/PR + eval gate; CD **[target]** — recommend when deploy cadence hurts |
| Supply chain | **Below bar** | gitleaks strong; no lockfile, `>=` deps, tag-pinned Actions, no Dependabot (**two-way** if approved) |
| OWASP ASVS L1 | **Partial** | HTTPS + JWT/admin key + basic auth; missing rate limit, CORS, write-path PII (**one-way** if enabling destructive filters without review) |
| Docs-as-code | **Meets** | STATUS/memory-contract/render gates; this scan fixed remaining CD/CORS/LifeGraph drift |
| Eval / model quality | **Partial** | synthetic gold + CI + weekly `eval-suite.yml`; live-stack eval + larger gold **[target]** |
| Cost governance | **Meets** | landed-cost ×1.3, flat-rate bias, caps |
| Agent autonomy safety | **Strong** | zero-cred runners, PR-only, reversibility classification |

## Deltas since 2026-06-10 baseline

- **Fixed then, still true:** CI real; STATUS snapshot gate; AGENTS practices honesty tags.
- **Resolved since baseline:** workflow stubs gone (`eval-suite` / `weekly-scan` / `ci-autofix` live); engineering-practices overclaim largely fixed in AGENTS (remaining overclaims were in architecture/setup/market-fit — fixed this PR).
- **Still open:** lockfile/Dependabot/SHA-pinning; CD; ADR 009 aspirational security; Mem0 `/metrics`; SLOs; RTO measurement.
- **New since baseline:** LifeGraph redesign freeze (STATUS 2026-06-16); remote MCP live for Claude/ChatGPT/Perplexity; lint/mypy debt accumulated and cleared this week.
- **Not applied:** any droplet/Terraform/retention/spend/data changes.
