# Weekly repo scan — 2026-06-13

> Automated headless pass (CI `weekly-scan.yml`). Fresh checkout, no droplet, no
> live credentials, no sibling repos.

## Summary

- All gates green after fixes: ruff, mypy, pytest (215/215, 88% cov), contract +
  STATUS snapshot, eval gate, pointer purity, contract render.
- Lint/type debt from recent script work (17 ruff + 3 mypy) — fixed in this PR
  (two-way doors).
- Doc drift from ADR 038 scrub: `"the operatorv"` placeholders in contract docs
  corrected to canonical `primary-user` / `example` bucket names.
- README + `architecture.md` no longer claim CD where deploy is still manual SSH.
- Supply chain (lockfile, action SHA-pin, Dependabot) and live CD remain open —
  recommend, not applied.

## Test / gate results

| Check | Result |
|---|---|
| `ruff check .` | **FAIL → fixed** (17 issues: import order, E501, SIM115, unused import) |
| `mypy src` | **FAIL → fixed** (3: `checker.py` None guard; `ingest.py` metadata typing) |
| `pytest` | **PASS** — 215 passed, 88.30% coverage (gate 80%) |
| `check_memory_contract.py` | **PASS** (extension repo skipped — not checked out) |
| `check_status_snapshot.py` | **PASS** (73 lines) |
| `render_contract.py --check` | **PASS** |
| `check_pointer_purity.py` | **PASS** |
| `run_eval_gate.py` | **PASS** (synthetic gold) |

## Findings fixed in this PR

1. **Ruff/mypy regressions** — `scripts/acceptance_probe.py`, `bulk_seed_importer.py`,
   `memory.py`, `memory_compaction.py`, `src/migration/categorizer.py`; mypy fixes in
   `src/health/checker.py` and `src/life_graph/ingest.py`.
2. **Contract doc drift (tenet 10)** — `docs/interfaces.md`, `docs/setup.md`,
   `docs/design/remote-mcp-oauth.md`, ADR 022: `"the operatorv"` → `primary-user` /
   `ai-memory-infra-backups-example` (ADR 038 scrub artifact).
3. **CI/CD overclaim** — `README.md` and `docs/architecture.md` now say CI is live;
   CD is manual SSH (`make deploy`), matching `AGENTS.md`.

## Findings needing an operator decision

| Finding | Recommendation |
|---|---|
| No lockfile; `pyproject.toml` uses `>=` ranges | **Two-way door** — add `uv.lock` or pip-tools lock + CI install from lock. Do when ready to own Renovate/Dependabot churn. |
| Actions referenced by tag (`@v4`/`@v5`), not SHA | **Two-way door** — pin SHAs once workflow set stabilizes (OpenSSF). Cheap batch PR. |
| No `cd.yml`; deploys manual SSH | **One-way door (ops)** — implement push-to-main deploy + health check + rollback when operator wants automated deploys. |
| ADR 009 / interfaces still describe PII filter, CORS, rate limiting as decided | **Two-way door (docs)** — AGENTS.md already tags these **[target]**; align ADR 009 header with "partially implemented" or add implementation backlog row. |
| `MEM0_DEFAULT_LLM_MODEL` silent fallback on droplet | **One-way door (live config)** — boot-time assert or alert; requires droplet change. |
| Historical COE/BUILD-JOURNEY entries still say `the operatorv` | **Ambiguous** — historical narrative vs tenet 10. Leave as dated history or footnote "scrub artifact"; contract-facing docs are now correct. |
| `docker-compose.yml` comment says Prometheus/Grafana "NOT here yet" but Phase 8 code exists | **Two-way door** — update compose comment or wire monitoring profile; code-side drift only. |

## Benchmark scorecard

| Area | Status | Gap | Next step |
|---|---|---|---|
| Twelve-Factor (config, stateless) | **Meets** | Sessions + env-based config | — |
| SRE (COE/postmortems) | **Exceeds for size** | No SLOs/error budgets | Define 1–2 SLOs when usage steady (**two-way door** doc) |
| SRE (SLOs) | **Below bar** | No availability/latency targets | Pick API uptime + backup freshness SLO (**two-way door**) |
| OWASP ASVS L1 (API) | **Partial** | No rate limit / CORS / PII filter live | Implement per ADR 009 phases (**one-way door** policy choices) |
| Supply chain | **Below bar** | No lockfile, unpinned actions, no Dependabot | Lockfile PR + Dependabot (**two-way door**) |
| Secret scanning | **Meets (local)** | gitleaks pre-commit; not in CI workflow | Optional: add gitleaks to `ci.yml` (**two-way door**) |
| CI (DORA) | **Meets** | Full gate stack on every push/PR | — |
| CD (DORA) | **Below bar** | Manual SSH deploy | `cd.yml` when operator approves (**one-way door**) |
| Eval quality | **Partial** | Synthetic gold only; live-stack eval missing | Expand gold + live eval per BACKLOG (**two-way door** code) |
| Docs-as-code | **Meets** | STATUS, contract, pointer, render gates enforced | — |
| Backup/DR | **Meets** | RPO 24h + drill automation documented | Measure RTO once (**one-way door** — timed restore on droplet) |

## Deltas since last report (2026-06-10 baseline)

| Item | Baseline | This week |
|---|---|---|
| Tests | 107 passed | **215 passed** (+108) |
| Coverage | 93.69% | **88.30%** (more `src/` surface) |
| ruff / mypy at scan | 1 ruff fail; mypy clean | **17 ruff + 3 mypy** — fixed here |
| `eval-suite.yml` | Listed as stub | **Live** (Sun 09:00 IST + manual) |
| Phases 5–8 | Baseline noted stubs | **Core code landed** (migration, life_graph, eval, observability) |
| Stub workflows (`cd`, `backup-verify`, `docker-build`) | Recommended delete | **Removed** (per maturity pass) |
| Neo4j doc drift | Parked | **Corrected** in architecture (ADR 032) |
| `primary-user` in contract docs | Baseline didn't flag scrub artifact | **Fixed** `"the operatorv"` in 4 contract-facing files |
