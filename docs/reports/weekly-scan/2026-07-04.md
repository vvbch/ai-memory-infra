# Weekly scan — 2026-07-04

> Automated headless run (CI checkout only; no droplet/API credentials).

## Summary

- **Gates:** ruff, mypy, pytest (220/220, 89% cov), memory-contract, STATUS snapshot — all green after fixes.
- **Lint/type debt on main:** 17 ruff + 3 mypy failures accumulated since baseline; all fixed (two-way doors).
- **Doc drift:** three present-tense overclaims corrected (`architecture.md`, `market-fit-validation.md`, `lifegraph.md`).
- **Maturity vs baseline:** CI richer (contract render, pointer purity, eval gate); `eval-suite.yml` live; stub workflows gone; `src/` phases 5–8 implemented (no longer empty stubs).
- **Still below bar:** supply-chain pinning, CD, live-stack eval, SLOs, target security guardrails (PII/CORS/rate limit).

## Test / gate results

| Check | Result |
|---|---|
| `ruff check .` | **FAIL → PASS** (17 issues: I001 import order, E402 path-bootstrap, E501, SIM115, F401) |
| `mypy src` | **FAIL → PASS** (3: `checker.py` None guard; `ingest.py` metadata narrow) |
| `pytest` | **PASS** — 220 tests, 88.6% cov (gate 80%) |
| `check_memory_contract.py` | **PASS** (extension repo skipped — not checked out) |
| `check_status_snapshot.py` | **PASS** — STATUS.md 69 lines |

## Findings fixed in this PR (two-way doors)

1. **Lint sweep** — import ordering/unused imports in scripts + `src/mcp_proxy/`; line-length wraps; context managers for JSON reads (`bulk_seed_importer.py`, `memory_compaction.py`); `memory.py` path-bootstrap noqa for isort alias split.
2. **mypy strict** — `check_http_status` guards missing status; `ingest_memory_record` narrows metadata dict.
3. **`docs/architecture.md`** — subdomain table: "JWT auth; CORS allowlist" → "X-API-Key + JWT support; CORS allowlist **target** (ADR 009)".
4. **`docs/market-fit-validation.md`** — remote MCP live (not "planned"); safety guardrails partial; CI not CD.
5. **`docs/design/lifegraph.md`** — "stubs" → accurate "in-memory POC with tests".

## Findings needing an operator decision

| Finding | Recommendation |
|---|---|
| No lockfile; `pyproject.toml` uses `>=` ranges | Add `uv.lock` (or pip-tools) + Dependabot/Renovate. **Two-way door** — recommend next infra session. |
| GitHub Actions pinned by tag (`@v4`), not SHA | Pin to commit SHAs once workflow set stabilizes (OpenSSF). One-way-ish policy — operator ack. |
| PII filter, CORS allowlist, Caddy rate limiting still target | Implement per ADR 009 Phase 9/security **or** leave as target with BACKLOG dates. Recommend implement before public demo. |
| `MEM0_DEFAULT_LLM_MODEL` silent fallback on droplet | Boot-time assert or alert. **Touches live config** — operator-only. |
| Manual SSH deploy; no `cd.yml` | Build push-to-main CD with health check + rollback when operator ready (one-way door: deploy automation). |
| Live-stack eval (expanded gold, real Mem0) | Keep synthetic gate blocking CI; schedule live eval after premise test. |
| No SLOs / error budgets; RTO never timed | Define 1–2 SLOs (API availability, backup freshness) + one timed restore drill when usage stabilizes. |
| STATUS.md content stale (last updated 2026-06-16) | Shape gate passes; narrative is 18 days old. Refresh on next working session — not auto-edited here (session checkpoint, not scan scope). |

## Benchmark scorecard

| Area | Status | Gap | Next step |
|---|---|---|---|
| Twelve-Factor | **Meets** | — | Keep env-based config; stateless agent sessions (tenet 16) |
| SRE / incidents | **Strong for size** | No SLOs/error budgets | Define availability + backup SLOs (**operator**) |
| Backup / DR | **Meets** | RTO unmeasured | Time one full restore drill (**operator**, droplet) |
| CI (DORA) | **Meets** | No CD | `cd.yml` with health check (**one-way**, when ready) |
| Supply chain | **Below bar** | No lockfile, action SHA pins, Dependabot | Lockfile + Renovate (**two-way**, cheap) |
| API security (ASVS L1) | **Partial** | PII filter, rate limit, CORS | Implement ADR 009 targets or document timeline |
| Docs-as-code | **Meets** | — | Contract gates + weekly scan enforce tenet 10 |
| Eval / model quality | **Partial** | Synthetic gold only in CI | Live-stack eval post-premise test (**operator**) |
| Secret scanning | **Partial** | gitleaks in pre-commit locally; not in CI workflow | Add gitleaks step to `ci.yml` (**two-way**) |
| Cost governance | **Meets** | — | Landed-cost convention, flat-rate bias |

## Redundancy / consistency notes

- **Resolved this scan:** architecture subdomain auth vs `AGENTS.md`/`interfaces.md` §5; market-fit checklist vs reality; lifegraph "stubs" vs implemented POC.
- **Intentional split (OK):** STATUS (snapshot) vs BUILD-LOG/BUILD-JOURNEY (history); public vs private interview narrative.
- **Ambiguous — no edit:** `interfaces.md` §5 prose still lists JWT+CORS as deployed config while noting guardrails are "partly aspirational" — consistent enough; a future pass could tighten wording to match `AGENTS.md` exactly.
- **Ambiguous — no edit:** `docs/decisions/009-security-guardrails-architecture.md` describes full guardrail set as decided architecture; implementation status lives in BACKLOG — ADR-as-intent vs code-as-reality is deliberate.

## Deltas since 2026-06-10 baseline

| Item | Baseline | This scan |
|---|---|---|
| pytest count | 107 | 220 |
| Coverage | 93.7% | 88.6% (still >80%; more code, honest denominator) |
| ruff on main | 1 known failure | 17 accumulated — **fixed** |
| mypy | clean | 3 regressions — **fixed** |
| CI jobs | ruff + mypy + pytest + 2 gates | + render_contract, pointer_purity, eval gate |
| `eval-suite.yml` | stub (baseline said delete) | **Live** — Sunday 03:30 UTC |
| Stub workflows (`cd`, `backup-verify`, `docker-build`) | recommended delete | **Gone** |
| `src/` eval/migration/life_graph | empty stubs | **Implemented** with tests |
| AGENTS security claims | overstated (baseline) | **Honest** partial/target tags |
| Weekly scan automation | new | **Second report** (this file) |
