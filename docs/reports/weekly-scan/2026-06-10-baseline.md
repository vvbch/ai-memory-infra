# Repo scan & architecture benchmark — 2026-06-10 (baseline)

> First (manual, in-session) run of the pass that `weekly-scan.yml` will repeat
> every Saturday 09:00 IST. Future reports land in this folder and diff against
> the previous one.

## Summary

- Tests green (107/107, 94% coverage); one ruff failure found and fixed at baseline scan time.
- One systemic finding → COE: STATUS.md had drifted into a second log
  (`docs/coe/2026-06-10-status-snapshot-log-drift.md`); shape gate now enforces it.
- Biggest credibility gap: `AGENTS.md` engineering-practice claims (CI, eval,
  security) ran ahead of implementation. CI is now real; eval/security/CD are not.
- Benchmark: strong on DR, incident practice, docs-as-code, cost governance;
  below bar on supply-chain pinning, SLOs, CD, and the eval framework.
- All fixes applied this session were two-way doors; one-way doors listed below
  were NOT applied.

## Test / gate results (at scan time)

| Check | Result |
|---|---|
| pytest | 107 passed, coverage 93.69% (gate 80%) |
| ruff | 1 failure (`check_memory_contract.py` E501) — **fixed** |
| mypy --strict (src) | clean |
| memory-contract gate | OK |
| STATUS snapshot gate | new — failing before trim, OK after |

Note: lint was failing on main with nothing to catch it — pre-commit checks
integrity/secrets/STATUS but not lint. Now caught by `ci.yml` on every push.

## Findings fixed in this session (two-way doors)

1. **STATUS.md log drift** — trimmed 1,343 → ~140 lines; history preserved in
   BUILD-LOG + git; `check_status_snapshot.py` (TDD, 9 tests) wired into
   pre-commit gate 3 + CI; AGENTS.md DoD row clarified ("overwrite means replace").
2. **ruff E501** in `check_memory_contract.py`.
3. **CI stub → real workflow** (`ci.yml`): ruff + mypy + pytest + both contract
   gates on push/PR.
4. **No reaction to failures between passes** → `ci-autofix.yml` (agent fixes CI
   failures on main via PR).
5. **No recurring review** → `weekly-scan.yml` (Sat 09:00 IST) + versioned
   prompts in `docs/automation/`.

## Findings needing an operator decision (NOT applied)

| Finding | Recommendation |
|---|---|
| `AGENTS.md` "Engineering practices" states JWT auth, PII filter, rate limiting, CORS allowlist as present-tense; live API uses a single admin `X-API-Key`, no PII filter | Two options: implement (Phase-aligned) or reword to "target state" with a checklist. Recommend reword now (5 min, honest docs), implement per phase plan. Reword is two-way but touches the project's "constitution" — operator call. |
| `cd.yml`, `eval-suite.yml`, `backup-verify.yml`, `docker-build.yml` are comment-only stubs | Delete the stubs (they imply capability that doesn't exist) and recreate when built, or implement. Recommend delete; BACKLOG already tracks the work. |
| Dependencies unpinned (no lockfile; `>=` ranges in pyproject) | Add a lockfile (`uv lock` or `pip-tools`) + Dependabot/Renovate. Supply-chain hygiene gap vs industry baseline. |
| GitHub Actions referenced by tag (`@v4`/`@v7`), not SHA | Pin to commit SHAs (OpenSSF guidance) once workflows stabilize. |
| `gpt-4.1-nano` silent fallback if `MEM0_DEFAULT_LLM_MODEL` unset (STATUS risk, long-standing) | Add a boot-time assert or alert on the live config. Touches droplet — out of scan scope. |
| Mem0 ships no graph-memory code; `docs/architecture.md`/`AGENTS.md` overstate Neo4j's current role | Already parked as P1 `[docs-drift]` in BACKLOG — do it next docs session. |

## Benchmark scorecard (vs industry standards)

| Area | Standard | Status | Gap / next step |
|---|---|---|---|
| Stateless processes & config | Twelve-Factor | **Meets** | env-based config, stateless sessions (tenet 16), disposable infra |
| Incident management | Google SRE postmortems / Amazon COE | **Exceeds for size** | 9 blameless COEs w/ 5-whys + benchmark section; gap: no SLOs/error budgets — define 1-2 SLOs (API availability, backup freshness) once usage is real |
| Backup / DR | 3-2-1, RPO/RTO, drills | **Meets** | RPO 24h + dead-man's-switch + monthly *proven* restore drill; gap: RTO never measured — time a full restore once |
| CI maturity | DORA | **Now baseline** | CI on every change (new); no CD → deploys are manual SSH; next: `cd.yml` with health check + rollback (already in AGENTS target) |
| Supply chain | OpenSSF / SLSA basics | **Below bar** | secret-scanning strong (gitleaks); missing lockfile, dep pinning, action SHA-pinning, Dependabot |
| API security | OWASP ASVS L1 | **Partial** | HTTPS + auth on all routes + least-priv backup key; missing rate limiting, CORS allowlist, PII filter (claimed in docs — see decision table) |
| Docs-as-code | industry doc-lint practice | **Meets (new)** | doc contracts machine-enforced (STATUS gate, memory-contract gate, DoD trigger table) |
| Eval / model quality | LLM eval best practice | **Below bar (known)** | framework designed (ADR 007/014), `src/eval/` still stubs; BACKLOG'd behind the premise test — right sequencing, keep honest |
| Cost governance | FinOps | **Meets** | landed-cost convention, flat-rate bias, hard caps, pause path |
| Agent autonomy safety | emerging practice | **Strong** | zero-credential runners, PR-only, reversibility classification, no-gate-weakening rule |

## Redundancy / consistency notes

- STATUS vs BUILD-LOG duplication: resolved by the trim + gate (above).
- `docs/BUILD-JOURNEY.md` vs BUILD-LOG: intentional public/private split — keep,
  but the weekly scan will check they stay in agreement (tenet 10).
- `src/` placeholder modules (eval, migration, life_graph, observability,
  health) are empty files inflating the "implemented" impression and coverage
  denominator trivially; harmless but the weekly scan should flag if they're
  still empty when their phase starts.

## Deltas since last report

n/a — baseline.
