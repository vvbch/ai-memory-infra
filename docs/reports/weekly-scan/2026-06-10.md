# Weekly scan — 2026-06-10

> Automated headless run (CI, no droplet/credentials). Diff against
> `2026-06-10-baseline.md` (manual first pass, same day).

## Summary

- All gates green: ruff, mypy, pytest (31/31, 90% cov), memory-contract, STATUS shape.
- Fixed P1 `[docs-drift]`: Neo4j/Mem0 graph overstated across architecture docs, README,
  AGENTS.md, compose/Dockerfile comments, and market-fit checklist.
- AGENTS.md engineering-practice claims now distinguish live CI vs stub CD/eval workflows.
- No test/gate failures to fix; no code changes required.
- One-way doors unchanged from baseline (stub workflow deletion, lockfile, action SHA-pin,
  droplet Neo4j verify, ADR 005 supersession) — still need operator decisions.

## Test / gate results

| Check | Result |
|---|---|
| `ruff check .` | pass |
| `mypy src` | pass (29 files) |
| `pytest` | 31 passed, 90.22% coverage (gate 80%) |
| `check_memory_contract.py` | OK (extension repo skipped — not checked out) |
| `check_status_snapshot.py` | OK (137 lines) |

## Findings fixed in this PR (two-way doors)

1. **Neo4j / Mem0 graph doc drift (P1 BACKLOG)** — corrected in `docs/architecture.md`
   (diagram + subdomain auth row), `AGENTS.md` (architecture summary + engineering
   practices qualifiers), `README.md`, `infra/docker-compose.yml`,
   `infra/mem0-server.Dockerfile`, `docs/market-fit-validation.md` (honest ⚠️ rows).
   Auth wording now matches ADR 020 (`X-API-Key`), not JWT.
2. **BACKLOG** — `[docs-drift]` item updated to partial-done with remaining steps listed.

## Findings needing an operator decision (NOT applied)

| Finding | Recommendation |
|---|---|
| Droplet Neo4j empty-graph verify | SSH + `MATCH (n) RETURN count(n)` on live instance; closes P1 docs-drift. One-way if data exists — investigate first. |
| ADR 005 still claims Mem0 auto-namespace | Supersede or add "implementation status" section once droplet verify done. Recommend supersede → LifeGraph-only until Mem0 ships graph code. |
| Four stub workflows (`cd`, `eval-suite`, `backup-verify`, `docker-build`) | Delete stubs (honest repo) or implement. Baseline recommendation stands: delete; BACKLOG tracks build. |
| Dependencies unpinned (`>=` ranges, no lockfile) | Add `uv.lock` or `pip-tools` + Dependabot. Supply-chain gap. |
| GitHub Actions use version tags, not SHAs | Pin to commit SHAs per OpenSSF once workflows stabilize. |
| `gpt-4.1-nano` silent fallback if `MEM0_DEFAULT_LLM_MODEL` unset | Boot-time assert on droplet — out of CI scope. |
| `docs/planning/setup-prompt.md` interview script still says dual namespace | Historical planning artifact — annotate or update when ADR 005 settled; not blocking. |

## Benchmark scorecard

| Area | Status | Gap | Next step |
|---|---|---|---|
| Twelve-Factor (config, stateless) | **Meets** | CI lacks compose integration tests | Two-way: add compose-backed pytest job when Phase 5 starts |
| SRE / COE practice | **Exceeds for size** | No SLOs/error budgets | One-way: define API availability + backup-freshness SLOs after premise test |
| Backup / DR | **Meets** | RTO never timed | One-way: time a full restore drill once (operator) |
| CI / DORA | **Baseline** | CD manual; no deployment frequency metric | Two-way: implement `cd.yml` with health check + rollback |
| Supply chain | **Below bar** | No lockfile, dep pinning, action SHAs, Dependabot | Two-way: lockfile + Dependabot PR |
| API security (OWASP ASVS L1) | **Partial** | HTTPS + X-API-Key live; no PII/CORS/rate-limit | Two-way: implement ADR 009 guardrails per phase plan |
| Docs-as-code | **Meets** | `setup-prompt.md` stale on graph | Annotate when ADR 005 settled |
| Eval / model quality | **Below bar (known)** | `src/eval/` stubs; `eval-suite.yml` stub | Implement Phase 7; keep honest in docs (done this scan) |
| Cost governance | **Meets** | — | — |
| Agent autonomy safety | **Strong** | — | Zero-credential runner, PR-only, reversibility rules enforced |

## Redundancy / consistency notes

- STATUS vs BUILD-LOG: gate holding; no new drift.
- BUILD-JOURNEY already documented the Neo4j discovery (2026-06-10 entry) — aligned
  with fixes applied here.
- `setup-prompt.md` vs live architecture: intentional historical doc; flagged above.
- Empty `src/eval/`, `src/life_graph/`, etc. still inflate coverage denominator
  harmlessly — flag again when those phases start.

## Deltas since `2026-06-10-baseline.md`

- Tests: 22 → 31 (STATUS snapshot gate tests added since baseline).
- Gates: all green with zero fixes required (baseline had ruff E501 + STATUS trim).
- Doc drift: baseline listed as operator backlog; this scan applied the verified
  two-way-door doc fixes.
- `market-fit-validation.md`: corrected aspirational ✅ rows to honest ⚠️ status.
- Operator-decision table: unchanged items carried forward; droplet verify + ADR 005
  supersession called out explicitly as the remaining P1 docs-drift steps.
