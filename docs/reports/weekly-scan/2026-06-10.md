# Weekly scan — 2026-06-10

> Automated run (headless CI, fresh checkout, no droplet/credentials). Diff
> against `2026-06-10-baseline.md` (manual first pass, same day).

## Summary

- All gates green on first run — no code fixes required (31 tests, 90% coverage).
- Doc drift corrected in five files: CI reality, Neo4j graph claim, observability
  Phase 8 placement, market-fit checkmarks, subdomain auth notes.
- Stub workflows (`cd`, `eval-suite`, `backup-verify`, `docker-build`) still
  imply capability — operator decision to delete or implement.
- Supply chain and CD/eval maturity gaps unchanged from baseline; no one-way doors
  touched.
- Next operator step remains: set `CURSOR_API_KEY` + `PRIVATE_REPO_PAT` so this
  job can open its own PRs autonomously.

## Test / gate results

| Check | Result |
|---|---|
| `ruff check .` | ✅ pass |
| `mypy src` | ✅ pass (29 files) |
| `pytest` | ✅ 31 passed, 90.22% coverage (gate 80%) |
| `check_memory_contract.py` | ✅ OK (extension repo skipped — not checked out) |
| `check_status_snapshot.py` | ✅ OK (137 lines) |

## Findings fixed in this PR (two-way doors)

1. **`AGENTS.md` CI claim** — removed false "ephemeral Docker stack in CI"; noted
   target per ADR 007. Also corrected Neo4j summary to match verified mem0ai 2.0.4
   behaviour (BACKLOG P1).
2. **`README.md`** — "CI/CD" → CI live / CD planned; knowledge-graph line matches
   Neo4j reality; eval/observability marked Phases 7–8.
3. **`docs/architecture.md`** — Prometheus/Grafana moved to dashed Phase 8
   subgraph; Neo4j label + subdomain auth notes aligned with ADR 009/020 and
   `infra/docker-compose.yml` comments.
4. **`docs/market-fit-validation.md`** — eval, guardrails, and observability
   rows downgraded from ✅ to ⚠️ where only ADRs/design exist.

## Findings needing an operator decision

| Finding | Recommendation |
|---|---|
| Four stub workflows (`cd.yml`, `eval-suite.yml`, `backup-verify.yml`, `docker-build.yml`) | **Delete stubs** and recreate when built (BACKLOG tracks work). Stubs mislead readers and the benchmark scorecard. Two-way door; operator taste call on deleting vs keeping as placeholders. |
| `AGENTS.md` Engineering practices still states CD + weekly eval as present-tense targets | **Reword to "target state"** bullets (like the Security line already does) or implement. Recommend reword — 5 min, keeps constitution honest. |
| Dependencies unpinned (`>=` in `pyproject.toml`, no lockfile) | Add `uv.lock` or `pip-tools` output + Dependabot/Renovate. Two-way door; cheap hygiene win. |
| GitHub Actions use version tags (`@v4`/`@v7`), not commit SHAs | Pin to SHAs once workflow set stabilises (OpenSSF). Two-way door. |
| `infra/docker-compose.yml` + Dockerfile still comment "Mem0 graph + LifeGraph" / `mem0ai[graph]` | After droplet Neo4j empty-node check, strip dead `NEO4J_*` env vars or upgrade Mem0 when graph extra ships. **One-way door** until graph strategy ADR is updated. |
| `gpt-4.1-nano` silent fallback if `MEM0_DEFAULT_LLM_MODEL` unset | Boot-time assert on droplet. Out of CI scope — operator/droplet task. |

## Benchmark scorecard

| Area | Status | Gap | Next step |
|---|---|---|---|
| Twelve-Factor (config, stateless) | **Meets** | CI lacks compose integration tests | Add ephemeral compose job when Phase 7 eval needs it (**two-way**) |
| SRE / COE practice | **Exceeds for size** | No SLOs/error budgets | Define API availability + backup-freshness SLOs after premise test (**two-way**) |
| Backup / DR | **Meets** | RTO never measured | Time one full restore drill (**one-way** on live data — operator) |
| CI / DORA | **Baseline** | No CD; no compose in CI | Implement `cd.yml` with health check + rollback (**one-way** until tested on droplet) |
| Supply chain | **Below bar** | No lockfile, floating deps, tag-pinned actions | Lockfile + Dependabot + SHA-pin actions (**two-way**) |
| OWASP ASVS L1 (API) | **Partial** | No rate limit, PII filter, CORS hardening | Phase 1 hardening per ADR 009; Caddyfile documents rate-limit gap (**two-way** doc already done) |
| Docs-as-code | **Meets** | Neo4j claim was stale — fixed this pass | Weekly scan continues; add droplet-empty-graph confirmation to BACKLOG close-out |
| Eval / model quality | **Below bar (known)** | `src/eval/` stubs; `eval-suite.yml` stub | Phase 7 after premise test — correct sequencing |
| Secret scanning | **Meets** | gitleaks pre-commit + `.gitleaks.toml`; not in CI workflow | Optional: add gitleaks CI step (**two-way**) |
| Agent automation safety | **Strong** | Secrets unset → weekly job cannot self-PR yet | Operator sets `CURSOR_API_KEY` + `PRIVATE_REPO_PAT` (**one-way** credential) |

## Redundancy / consistency notes

- **STATUS vs BUILD-LOG:** gate still holding; no new log drift.
- **BUILD-JOURNEY vs BACKLOG:** both already acknowledge Neo4j/Mem0 graph gap —
  public docs now match (tenet 10).
- **`setup-prompt.md`:** still describes full target architecture (scaffold spec) —
  intentional; not a drift violation if read as spec, but interview/marketing
  excerpts there overstate live state — flag for a future docs pass, not changed
  here (large surface, low Saturday-morning value).
- **Stub `src/` modules** (eval, migration, life_graph, observability, health):
  still empty placeholders; coverage denominator inflated but harmless until their
  phases start.

## Deltas since `2026-06-10-baseline.md`

| Baseline | This run |
|---|---|
| ruff had 1 E501 failure (fixed manually) | ruff clean on arrival |
| 22 tests | 31 tests (+9 `check_status_snapshot` tests) |
| Doc drift listed as operator backlog | Five doc files patched in this PR |
| First report (n/a deltas) | Second report; automation path validated headless |
